const addValue = 8;
const defaultValue = 8;

const defaultBorderRadius = defaultValue;
const largeBorderRadius = defaultBorderRadius + addValue;

const borderRadius = {
  defaultBorderRadius,
  largeBorderRadius,
};

export default borderRadius;
